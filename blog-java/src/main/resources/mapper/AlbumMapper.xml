<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blog.mapper.AlbumMapper">

    <select id="getAlbumNames" resultType="String">
        select album_name from album where author_id=#{authorId};
    </select>

    <select id="getAlbums" resultType="blog.entity.Album">
        select * from album where author_id=#{authorId};
    </select>

    <select id="getIdByName" resultType="Integer">
        select id from album where author_id=#{authorId} and album_name=#{albumName};
    </select>

</mapper>
